# Upstream Provenance

- Upstream repository: `https://github.com/windmill-labs/windmill`
- Upstream tag: `v1.775.1`
- Upstream commit: `cfb8ca4391c848b9f841fab1fb844c9d00cd4ad6`
- Public fork repository: `https://github.com/nolyOne1/aiwise-windmill-zh`

## Source Availability

This fork is based on the public Windmill source tree at tag `v1.775.1` / commit `cfb8ca4391c848b9f841fab1fb844c9d00cd4ad6`.

Windmill's repository is published under AGPLv3 terms for the open-source source tree. This fork preserves public source availability by documenting the exact upstream baseline and the public fork location above.

## Future Upstream Comparisons

Use these commands to compare this fork against a future upstream tag:

```bash
git fetch origin --tags
git diff v1.775.1..vX.YYY.ZZ --stat
git log --oneline --decorate v1.775.1..vX.YYY.ZZ
git range-diff v1.775.1...vX.YYY.ZZ
```

Replace `vX.YYY.ZZ` with the future upstream tag you want to inspect.
